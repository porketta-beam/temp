# CAN SLIM 한국 주식 스크리닝 시스템 필수 라이브러리

# 데이터 수집
pykrx>=1.0.45          # 한국거래소 데이터
yfinance>=0.2.28       # 보조 데이터 소스 (선택)

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# 기술적 지표
ta>=0.10.2             # Technical Analysis 라이브러리
talib-binary>=0.4.26  # TA-Lib (선택, C++ 의존성)

# 시각화
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# 유틸리티
tqdm>=4.65.0           # 진행률 표시
openpyxl>=3.1.0        # Excel 파일 처리
xlsxwriter>=3.1.0      # Excel 출력
python-dotenv>=1.0.0   # 환경 변수 관리

# 웹 스크래핑 (선택)
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0

# 백테스팅 (선택)
quantstats>=0.0.62    # 포트폴리오 분석
empyrical>=0.5.5       # 성과 측정

# Jupyter 환경
jupyter>=1.0.0
ipywidgets>=8.0.0
nbformat>=5.9.0

# 데이터베이스 (선택)
sqlalchemy>=2.0.0
pymongo>=4.4.0

# 기계학습 (선택)
scikit-learn>=1.3.0
xgboost>=1.7.0
lightgbm>=4.0.0